# movierec 디렉토리 생성 및 이동
mkdir movierec
cd movierec/

# Dockerfile 및 docker-compose.yml 작성
gedit Dockerfile
gedit docker-compose.yml

# Docker 및 Docker Compose 버전 확인
docker --version
docker-compose --version

# Docker Compose로 이미지 빌드
docker-compose build

# Flask 애플리케이션 코드 작성
gedit app.py

# 필요한 패키지 목록 작성
gedit requirements.txt

# 템플릿 폴더 생성 및 index.html 작성
mkdir templates
cd templates/
gedit index.html
cd ..

# 정적 파일 폴더 생성
mkdir static

# Docker 이미지 빌드
docker build -t movierec .

# Docker 컨테이너 실행
docker run -d -p 5000:5000 movierec
